<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:background="@color/guniThemeBackground">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/guniThemeText">

        <EditText
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:inputType="textPersonName"
            android:ems="10"
            android:id="@+id/studyFragVideoSearchET"
            android:layout_weight="1"
            android:hint="Android prodramming.pdf"
            android:textColor="@color/guniThemeText"
            android:background="@color/guniThemeBackground"
            android:layout_marginBottom="2dp" />

        <Button
            android:text="Search"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/materialFragVideoSearchButton"
            android:layout_weight="1"
            android:background="@color/guniThemeBackground"
            android:textColor="@color/guniThemeText" />
    </LinearLayout>

    <ExpandableListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:id="@+id/materialFragVideoExpandableListView" />

</LinearLayout>